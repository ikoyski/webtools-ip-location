server:
  port: 8092

spring:
  application:
    name: webtools-ip-location

management:
  endpoints:
    web:
      exposure:
        include: health,info
  info:
    git:
      mode: full

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761

provider:
 default: IpApi
